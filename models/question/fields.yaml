# ===================================
#  Form Field Definitions
# ===================================

fields:
    category:
        label: Category
        type: dropdown
        emptyOption: -- select category --
        span: left
    question:
        label: Question
        type: markdown
    answer:
        label: Answer
        type: markdown
    image:
      label: Image
      type: fileupload
      mode: image
