# ===================================
#  Form Field Definitions
# ===================================

fields:
    category:
        label: Category
        type: relation
        emptyOption: -- select category --
        span: left
    question:
        label: Question
    answer:
        label: Answer
        type: textarea
    image:
      label: Image
      type: fileupload
      mode: image
