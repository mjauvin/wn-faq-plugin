# ===================================
#  Form Field Definitions
# ===================================

fields:
    category:
        label: Category
        type: dropdown
        emptyOption: -- select category --
        span: left
    question:
        label: Question
    answer:
        label: Answer
        type: textarea
    image:
      label: Image
      type: fileupload
      mode: image
