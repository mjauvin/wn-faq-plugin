# ===================================
#  Form Field Definitions
# ===================================

fields:
    name:
        label: 'studioazura.faq::lang.models.category.name'
        required: true
        span: auto
    image:
        label: 'studioazura.faq::lang.models.category.image'
        type: fileupload
        mode: image
        span: auto
